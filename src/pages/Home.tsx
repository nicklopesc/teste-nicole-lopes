import { Stack } from "@mui/material";
import Map from "../modules/map/components/Map";

const Home: React.FC = () => {
  return (
    <Stack>
      <Map />
    </Stack>
  );
};

export default Home;
